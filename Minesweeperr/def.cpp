//defining the default window width and height and ratio between resized and first window size
//I defined these here so that i can access them from anywhere in the program
//reason window width and window height are 16 at the beggining, is because a single tile is 16 pixels
//so when the person enters number of blocks we just multyply it by that
int windowh = 16;
int windoww = 16;
double ratiow = 1.0, ratioh = 1.0;